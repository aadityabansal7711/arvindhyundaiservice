# Shared secret the main app must send as `Authorization: Bearer <token>`
# on every request. Generate with: openssl rand -base64 32
GDMS_SERVICE_TOKEN="<random-base64-string>"

# Optional — Railway sets this automatically in production.
PORT=8080
